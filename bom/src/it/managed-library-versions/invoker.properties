# The shared version catalogue, consumed the way a real project consumes it: the
# parent for the build gates, an <import> of the BOM for the library versions.
# Six libraries declared, not one <version>. A dropped or misspelled entry fails
# here rather than in a consumer.
invoker.goals = verify
invoker.buildResult = success
