# The mirror image: the same tag, no javadoc.extra.args. Fails with "unknown tag.
# Unregistered custom tag?". If this ever builds green, the property is not what
# registers the tag and the test above proves nothing.
invoker.goals = javadoc:jar
invoker.buildResult = failure
