# The shared version catalogue. This project declares six libraries and not one
# <version>; every version comes from the parent's dependencyManagement. If an
# entry is removed or misspelled, resolution fails here rather than in a consumer.
invoker.goals = verify
invoker.buildResult = success
