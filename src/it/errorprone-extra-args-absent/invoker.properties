# The mirror image: identical sources, no errorprone.extra.args. If this ever
# builds green, the property is not what is switching the check off and the
# test above proves nothing.
invoker.goals = verify
invoker.buildResult = failure
