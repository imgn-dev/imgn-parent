# errorprone.extra.args must reach the -Xplugin argument, so a project can
# switch off a check that fights its conventions without overriding
# <compilerArgs> and losing the --add-exports set, the compile policy and
# NullAway with it.
invoker.goals = verify
invoker.buildResult = success
