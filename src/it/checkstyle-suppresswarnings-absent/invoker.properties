# The mirror image: the same 16-arm switch with no annotation. If this ever builds
# green, CyclomaticComplexity has stopped firing and the test above proves nothing.
invoker.goals = verify
invoker.buildResult = failure
