# Whatever palantir-java-format writes, checkstyle must accept. Both bind to
# validate, spotless first, so a disagreement is unfixable in a child project:
# spotless:apply rewrites the file moments before checkstyle:check reads it.
# No suppressions here on purpose — the two must agree unaided.
invoker.goals = verify
invoker.buildResult = success
