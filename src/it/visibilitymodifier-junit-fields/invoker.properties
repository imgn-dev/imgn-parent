# checkstyle runs over test sources too (includeTestSourceDirectory). JUnit 5
# rejects a private @TempDir or @RegisterExtension field, so VisibilityModifier
# and the framework contradict each other unless those annotations are exempt.
# A rule aimed at production fields must not reshape tests.
invoker.goals = verify
invoker.buildResult = success
